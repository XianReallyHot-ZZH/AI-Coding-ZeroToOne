# SpecKit 套件分析与实践指南

> 本文档全面分析 SpecKit 方法论的各个命令组件，帮助理解其设计理念、工作流程和最佳实践。

---

## 目录

1. [概述](#概述)
2. [核心理念](#核心理念)
3. [完整工作流](#完整工作流)
4. [命令详解](#命令详解)
   - [speckit.constitution](#1-speckitconstitution---项目宪法)
   - [speckit.specify](#2-speckitspecify---功能规格)
   - [speckit.clarify](#3-speckitclarify---需求澄清)
   - [speckit.plan](#4-speckitplan---技术计划)
   - [speckit.tasks](#5-speckittasks---任务拆解)
   - [speckit.checklist](#6-speckitchecklist---需求检查清单)
   - [speckit.analyze](#7-speckitanalyze---一致性分析)
   - [speckit.implement](#8-speckitimplement---代码实现)
   - [speckit.taskstoissues](#9-speckittaskstoissues---任务转issues)
5. [命令速查表](#命令速查表)
6. [最佳实践](#最佳实践)
7. [文件结构](#文件结构)

---

## 概述

**SpecKit** 是一套结构化的软件开发方法论，通过 AI 辅助将自然语言需求转化为可执行的代码。其核心思想是：

```
业务需求 → 规格文档 → 技术设计 → 任务清单 → 代码实现
```

### 设计目标

| 目标 | 说明 |
|------|------|
| **减少返工** | 在编码前充分澄清需求 |
| **提高质量** | 通过多层次验证确保一致性 |
| **AI友好** | 每个步骤产出可被AI理解和执行的文档 |
| **团队协作** | 支持多人协作和进度追踪 |

---

## 核心理念

### 1. 渐进式细化

```
模糊的业务想法
    ↓ clarify
清晰的需求规格
    ↓ plan
具体的技术设计
    ↓ tasks
可执行的任务清单
    ↓ implement
运行的代码
```

### 2. 关注点分离

| 阶段 | 关注点 | 受众 |
|------|--------|------|
| specify | WHAT & WHY | 业务干系人 |
| plan | HOW | 技术团队 |
| tasks | WHEN & WHO | 开发者 |
| implement | CODE | 机器/AI |

### 3. 质量门禁

每个阶段都有验证机制：
- `clarify` - 消除需求模糊
- `checklist` - 验证需求质量
- `analyze` - 检查跨工件一致性
- `constitution` - 确保遵循项目原则

---

## 完整工作流

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          SpecKit 完整工作流                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────┐                                                    │
│  │   constitution  │ ← 项目宪法（一次设置，持续约束）                      │
│  └────────┬────────┘                                                    │
│           ↓                                                             │
│  ┌─────────────────┐                                                    │
│  │    specify      │ ← 从自然语言创建功能规格                             │
│  │   (spec.md)     │                                                    │
│  └────────┬────────┘                                                    │
│           ↓                                                             │
│  ┌─────────────────┐                                                    │
│  │    clarify      │ ← 消除规格中的模糊性（最多5个问题）                    │
│  └────────┬────────┘                                                    │
│           ↓                                                             │
│  ┌─────────────────┐                                                    │
│  │      plan       │ ← 生成技术设计文档                                   │
│  │  (plan.md等)    │   - research.md                                     │
│  │                 │   - data-model.md                                   │
│  │                 │   - contracts/                                      │
│  └────────┬────────┘                                                    │
│           ↓                                                             │
│  ┌─────────────────┐                                                    │
│  │   checklist     │ ← 生成需求质量检查清单                               │
│  │ (checklists/)   │                                                    │
│  └────────┬────────┘                                                    │
│           ↓                                                             │
│  ┌─────────────────┐                                                    │
│  │     tasks       │ ← 将设计拆分为可执行任务                              │
│  │   (tasks.md)    │                                                    │
│  └────────┬────────┘                                                    │
│           ↓                                                             │
│  ┌─────────────────┐                                                    │
│  │     analyze     │ ← 跨工件一致性检查（只读分析）                         │
│  └────────┬────────┘                                                    │
│           ↓                                                             │
│  ┌─────────────────┐                                                    │
│  │    implement    │ ← 执行任务，生成代码                                 │
│  │   (源代码文件)   │                                                    │
│  └────────┬────────┘                                                    │
│           ↓                                                             │
│  ┌─────────────────┐                                                    │
│  │ taskstoissues   │ ← 可选：转换为GitHub Issues便于团队协作              │
│  └─────────────────┘                                                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 命令详解

### 1. speckit.constitution - 项目宪法

**文件位置**: `.specify/memory/constitution.md`

**作用**: 定义项目的核心原则、约束和治理规则，是整个项目的"根本大法"。

#### 核心概念

```
Constitution = 核心原则 + 约束条件 + 治理规则
```

#### 执行流程

```
加载现有宪法 → 收集/推导值 → 起草更新 → 一致性传播 → 生成影响报告 → 验证 → 写入
```

#### 版本规则

| 类型 | 触发条件 | 示例 |
|------|----------|------|
| MAJOR | 不兼容的治理/原则移除 | 1.0.0 → 2.0.0 |
| MINOR | 新增原则/章节 | 1.0.0 → 1.1.0 |
| PATCH | 澄清、措辞修复 | 1.0.0 → 1.0.1 |

#### 宪法示例

```markdown
# Project Constitution

**Version**: 1.0.0
**Ratified**: 2024-01-15

## Principles

### P1: Security First
All user-facing APIs MUST use HTTPS.
All passwords MUST be hashed with bcrypt.

**Rationale**: Security breaches can destroy user trust.

### P2: Test Coverage
All new code MUST have minimum 80% test coverage.

**Rationale**: Tests prevent regressions.

## Governance

- **Amendment Process**: PR with 2 approvals
- **Compliance Review**: Weekly automated checks
```

#### 使用方式

```bash
/speckit.constitution                    # 交互式创建/更新
/speckit.constitution 添加安全原则：所有API必须使用HTTPS
```

---

### 2. speckit.specify - 功能规格

**产出文件**: `specs/{branch}/spec.md`

**作用**: 从自然语言描述创建功能规格说明书，定义"做什么"和"为什么做"。

#### 核心流程

```
用户输入功能描述 → 生成分支 → 创建规格文件 → 质量验证 → 完成
```

#### 关键步骤

1. **生成分支名称**: 从功能描述提取关键词（2-4个词）
   - "添加用户认证" → `user-auth`
   - "实现OAuth2集成" → `oauth2-api-integration`

2. **检查并创建分支**: 检查远程/本地分支，运行脚本创建

3. **填写规格模板**: 使用 `spec-template.md` 填写
   - 用户场景与测试
   - 功能需求（必须可测试）
   - 成功标准（可量化、技术无关）
   - 关键实体

4. **质量验证**: 创建 `checklists/requirements.md`

#### 核心原则

| 原则 | 说明 |
|------|------|
| 关注 WHAT & WHY | 描述用户需要什么，为什么需要 |
| 避免 HOW | 不涉及技术栈、API、代码结构 |
| 可测试性 | 每个需求都必须可验证 |
| 业务导向 | 为业务干系人编写 |
| 限制澄清 | 最多3个 `[NEEDS CLARIFICATION]` |

#### 使用方式

```bash
/speckit.specify 添加一个用户登录功能，支持邮箱和手机号登录
```

---

### 3. speckit.clarify - 需求澄清

**作用**: 在技术计划之前识别并消除规格文档中的模糊性。

#### 核心目标

```
模糊需求 → 精准问答 → 更新规格 → 清晰可执行
```

#### 覆盖度扫描分类

| 分类 | 检查内容 |
|------|----------|
| 功能范围与行为 | 核心目标、成功标准、用户角色 |
| 领域与数据模型 | 实体、属性、关系、生命周期 |
| 交互与UX流程 | 用户旅程、错误/空/加载状态 |
| 非功能质量属性 | 性能、可扩展性、安全性、合规性 |
| 集成与外部依赖 | 外部服务、失败模式、数据格式 |
| 边缘情况与失败处理 | 负面场景、限流、冲突解决 |
| 约束与权衡 | 技术约束、明确权衡 |
| 术语与一致性 | 术语表、避免的同义词 |
| 完成信号 | 验收标准可测试性 |

#### 提问规则

- 最多 **5 个问题/会话**，最多 **10 个问题/全流程**
- 每个问题必须是多选题或短答案（≤5词）
- 提供推荐答案，用户可快速接受

#### 交互示例

```markdown
**Recommended:** Option B - 支持多种登录方式可提高用户转化率。

| Option | Description |
|--------|-------------|
| A | 仅邮箱 |
| B | 邮箱 + 手机号 |
| C | 邮箱 + 手机号 + OAuth |

You can reply with "A", "B", "yes" (accept recommendation), or custom answer.
```

#### 使用方式

```bash
/speckit.clarify
```

---

### 4. speckit.plan - 技术计划

**产出文件**:
- `plan.md` - 技术计划
- `research.md` - 技术研究
- `data-model.md` - 数据模型
- `contracts/` - 接口契约
- `quickstart.md` - 快速开始

**作用**: 将功能规格转化为技术实现计划，定义"怎么做"。

#### 两阶段工作流

##### Phase 0: Outline & Research

| 任务 | 说明 |
|------|------|
| 提取未知项 | 从 Technical Context 中找出 `NEEDS CLARIFICATION` |
| 派发研究任务 | 为每个技术选择找最佳实践 |
| 生成 research.md | 记录决策、理由、备选方案 |

**research.md 格式**:
```markdown
## Decision: [选择的方案]
## Rationale: [为什么选它]
## Alternatives considered: [评估过哪些其他方案]
```

##### Phase 1: Design & Contracts

| 产出物 | 说明 |
|--------|------|
| `data-model.md` | 实体、字段、关系、验证规则、状态转换 |
| `/contracts/` | 接口契约（API、CLI、UI等） |
| `quickstart.md` | 快速开始指南 |

#### Constitution Check

在计划开始和结束时都要检查项目宪法：
- 如果违反约束且无正当理由 → **ERROR**
- 确保技术选择符合项目原则

#### 使用方式

```bash
/speckit.plan
```

---

### 5. speckit.tasks - 任务拆解

**产出文件**: `tasks.md`

**作用**: 将技术计划转化为可执行的任务清单。

#### 任务格式

```text
- [ ] [TaskID] [P?] [Story?] Description with file path
```

| 组件 | 说明 | 示例 |
|------|------|------|
| `- [ ]` | Markdown 复选框 | `- [ ]` |
| `TaskID` | 任务编号 | `T001` |
| `[P]` | 可并行标记（可选） | `[P]` |
| `[Story]` | 用户故事标签 | `[US1]` |
| Description | 具体操作 + 文件路径 | `Create User model in src/models/user.py` |

#### 正确示例

```markdown
- [ ] T001 Create project structure per implementation plan
- [ ] T005 [P] Implement auth middleware in src/middleware/auth.py
- [ ] T012 [P] [US1] Create User model in src/models/user.py
- [ ] T014 [US1] Implement UserService in src/services/user_service.py
```

#### 阶段结构

```
Phase 1: Setup           → 项目初始化
Phase 2: Foundational    → 阻塞性前置任务
Phase 3: User Story 1    → P1 优先级用户故事
Phase 4: User Story 2    → P2 优先级用户故事
...
Final:   Polish          → 收尾与横切关注点
```

#### 核心原则

| 原则 | 说明 |
|------|------|
| 按故事组织 | 任务围绕用户故事，而非技术层 |
| 独立可测试 | 每个故事完成后可独立验证 |
| MVP 优先 | 先实现 P1 故事，增量交付 |
| 并行友好 | `[P]` 标记可并行任务 |

#### 使用方式

```bash
/speckit.tasks
```

---

### 6. speckit.checklist - 需求检查清单

**产出文件**: `checklists/{domain}.md`

**作用**: 创建"需求文档的单元测试"——验证需求的质量、清晰度和完整性。

#### 核心概念

```
Checklist = "英文写成的需求的单元测试"
```

**关键区分**:

| ❌ 不是（测试实现） | ✅ 是（测试需求质量） |
|---------------------|----------------------|
| "验证按钮点击正常" | "按钮点击行为是否明确定义？" |
| "测试错误处理工作" | "所有错误场景是否有需求覆盖？" |
| "确认API返回200" | "API成功响应格式是否规范？" |

#### 质量维度

| 维度 | 检查内容 |
|------|----------|
| **Completeness** | 所有必要需求是否存在？ |
| **Clarity** | 需求是否明确无歧义？ |
| **Consistency** | 需求之间是否一致？ |
| **Measurability** | 需求是否可客观验证？ |
| **Coverage** | 所有场景/边缘情况是否覆盖？ |

#### 检查项格式

```markdown
- [ ] CHK001 Are [requirement type] defined for [scenario]? [Dimension, Spec §X.Y]
- [ ] CHK002 Is [vague term] quantified with specific criteria? [Clarity, Gap]
- [ ] CHK003 Are requirements consistent between [A] and [B]? [Consistency]
```

#### 示例对比

**❌ 错误**:
```markdown
- [ ] CHK001 Verify landing page displays 3 episode cards
- [ ] CHK002 Test hover states work correctly
```

**✅ 正确**:
```markdown
- [ ] CHK001 Are the number and layout of featured episodes explicitly specified? [Completeness]
- [ ] CHK002 Are hover state requirements consistently defined for all interactive elements? [Consistency]
```

#### 使用方式

```bash
/speckit.checklist
```

---

### 7. speckit.analyze - 一致性分析

**作用**: 在实现之前检测 spec.md、plan.md、tasks.md 之间的不一致。

**关键约束**: **严格只读**，不修改任何文件

#### 六类检测

| 检测类型 | 说明 |
|----------|------|
| **A. 重复检测** | 近似重复的需求 |
| **B. 模糊检测** | 模糊形容词、未解决占位符 |
| **C. 欠规范** | 缺失对象或可测量结果 |
| **D. 宪法对齐** | 违反 MUST 原则 |
| **E. 覆盖缺口** | 需求无任务、任务无需求 |
| **F. 不一致** | 术语漂移、实体缺失、顺序矛盾 |

#### 严重性分级

| 级别 | 触发条件 |
|------|----------|
| 🔴 **CRITICAL** | 违反宪法 MUST、缺失核心工件、需求零覆盖 |
| 🟠 **HIGH** | 重复/冲突需求、模糊安全/性能属性 |
| 🟡 **MEDIUM** | 术语漂移、非功能需求无任务覆盖 |
| 🟢 **LOW** | 风格/措辞改进、不影响执行的冗余 |

#### 分析报告格式

```markdown
## Specification Analysis Report

| ID | Category | Severity | Location(s) | Summary | Recommendation |
|----|----------|----------|-------------|---------|----------------|
| D1 | Constitution | 🔴 CRITICAL | plan.md:L78 | API未使用HTTPS | 修改为HTTPS |

**Metrics:**
- Requirements: 12
- Tasks: 24
- Coverage: 83%
- Critical Issues: 1
```

#### 使用方式

```bash
/speckit.analyze
```

---

### 8. speckit.implement - 代码实现

**作用**: 执行任务清单，将设计文档转化为实际代码。

#### 核心流程

```
检查前置条件 → 验证检查清单 → 加载上下文 → 验证项目配置 → 逐阶段执行任务 → 标记完成
```

#### 检查清单验证

扫描 `checklists/` 目录：

```text
| Checklist   | Total | Completed | Status |
|-------------|-------|-----------|--------|
| ux.md       | 12    | 12        | ✓ PASS |
| test.md     | 8     | 5         | ✗ FAIL |
```

- 有未完成项 → 询问用户是否继续
- 全部完成 → 自动进入实现

#### 项目设置验证

自动检测并创建/验证 ignore 文件：
- `.gitignore`
- `.dockerignore`
- `.eslintignore`
- `.prettierignore`
- 等

#### 执行顺序

```
1. Setup（初始化）      → 项目结构、依赖、配置
2. Tests（如需要）      → 先写测试（TDD）
3. Core（核心开发）     → Models → Services → CLI → Endpoints
4. Integration（集成）  → 数据库、中间件、日志、外部服务
5. Polish（收尾）       → 单元测试、性能优化、文档
```

#### 进度跟踪

- 每完成一个任务报告一次
- 完成的任务标记为 `[X]`
- 顺序任务失败 → 停止执行
- 并行任务失败 → 继续其他任务

#### 使用方式

```bash
/speckit.implement
```

---

### 9. speckit.taskstoissues - 任务转Issues

**作用**: 将 tasks.md 中的任务批量转换为 GitHub Issues。

**关键约束**: 只支持 GitHub 仓库

#### 执行流程

```
检查前置条件 → 获取Git远程地址 → 验证GitHub URL → 批量创建Issues
```

#### 安全约束

| 约束 | 说明 |
|------|------|
| 仅GitHub | 只支持GitHub仓库 |
| URL验证 | 必须匹配远程origin |
| 不跨仓库 | 绝不在其他仓库创建Issues |

#### 使用场景

| 场景 | 推荐 |
|------|------|
| 个人开发 | 直接用 `/speckit.implement` |
| 团队协作 | 先用 `/speckit.taskstoissues` 分配任务 |
| 需要追踪 | 用 Issues 追踪进度和状态 |

#### 使用方式

```bash
/speckit.taskstoissues
```

---

## 命令速查表

| 命令 | 产出 | 作用 | 执行时机 |
|------|------|------|----------|
| `/speckit.constitution` | `constitution.md` | 定义项目原则 | 项目初始化时 |
| `/speckit.specify` | `spec.md` | 创建功能规格 | 开始新功能时 |
| `/speckit.clarify` | 更新 `spec.md` | 消除需求模糊 | specify 后 |
| `/speckit.plan` | `plan.md`, `research.md`, `data-model.md` | 技术设计 | clarify 后 |
| `/speckit.tasks` | `tasks.md` | 任务拆解 | plan 后 |
| `/speckit.checklist` | `checklists/*.md` | 需求质量检查 | 任意阶段 |
| `/speckit.analyze` | 分析报告 | 一致性检查 | tasks 后 |
| `/speckit.implement` | 源代码 | 代码实现 | analyze 后 |
| `/speckit.taskstoissues` | GitHub Issues | 团队协作 | tasks 后 |

---

## 最佳实践

### 推荐工作流

```bash
# 1. 项目初始化（一次性）
/speckit.constitution

# 2. 开始新功能
/speckit.specify 添加用户登录功能，支持邮箱和手机号登录

# 3. 澄清需求
/speckit.clarify

# 4. 技术设计
/speckit.plan

# 5. 生成需求检查清单
/speckit.checklist

# 6. 任务拆解
/speckit.tasks

# 7. 一致性分析
/speckit.analyze

# 8a. 直接实现（个人开发）
/speckit.implement

# 8b. 转换为Issues（团队协作）
/speckit.taskstoissues
```

### 关键原则

1. **不要跳过 clarify** - 提前澄清比后期返工更高效
2. **重视 constitution** - 它约束所有后续决策
3. **使用 analyze** - 实现前检查一致性
4. **完成 checklist** - 检查清单未完成时谨慎继续
5. **保持 WHAT/HOW 分离** - specify 不涉及技术，plan 不涉及业务

### 常见问题

| 问题 | 解决方案 |
|------|----------|
| 需求模糊 | 运行 `/speckit.clarify` |
| 技术选型困难 | 检查 `research.md` |
| 任务遗漏 | 运行 `/speckit.analyze` |
| 宪法冲突 | 运行 `/speckit.constitution` 更新 |

---

## 文件结构

```
project/
├── .specify/
│   ├── memory/
│   │   └── constitution.md        # 项目宪法
│   ├── templates/
│   │   ├── spec-template.md       # 规格模板
│   │   ├── plan-template.md       # 计划模板
│   │   ├── tasks-template.md      # 任务模板
│   │   └── checklist-template.md  # 检查清单模板
│   └── scripts/
│       └── powershell/
│           ├── create-new-feature.ps1
│           ├── setup-plan.ps1
│           ├── check-prerequisites.ps1
│           └── update-agent-context.ps1
│
├── specs/
│   └── {branch}/
│       ├── spec.md                # 功能规格
│       ├── plan.md                # 技术计划
│       ├── research.md            # 技术研究
│       ├── data-model.md          # 数据模型
│       ├── quickstart.md          # 快速开始
│       ├── tasks.md               # 任务清单
│       ├── contracts/             # 接口契约
│       │   └── api.yaml
│       └── checklists/            # 检查清单
│           ├── requirements.md
│           ├── ux.md
│           └── security.md
│
└── .claude/
    └── commands/
        ├── speckit.constitution.md
        ├── speckit.specify.md
        ├── speckit.clarify.md
        ├── speckit.plan.md
        ├── speckit.tasks.md
        ├── speckit.checklist.md
        ├── speckit.analyze.md
        ├── speckit.implement.md
        └── speckit.taskstoissues.md
```

---

## 总结

SpecKit 是一套完整的 AI 辅助软件开发方法论，其核心价值在于：

1. **结构化流程** - 从需求到实现的清晰路径
2. **质量门禁** - 多层次验证确保一致性
3. **AI友好** - 每个步骤产出可被AI理解和执行的文档
4. **渐进细化** - 逐步从模糊到精确
5. **关注点分离** - 业务需求与技术实现清晰分开

通过遵循 SpecKit 工作流，团队可以显著减少返工、提高代码质量，并实现更高效的 AI 辅助开发。

---

*文档生成时间: 2024-02-27*
*SpecKit 版本: 基于 .claude/commands/ 分析*
