Based on my research, I have a clear picture of MotherDuck's design identity: **yellow + blue palette, illustration-heavy, minimal SaaS, playful yet technical, duck mascot branding**. Let me build a comprehensive style guide and demo page.这是一份完整的 MotherDuck 风格复刻页面，包含设计规范速查。以下是核心设计要素的总结：

---

## MotherDuck 设计风格核心要素

### 🎨 色彩体系
| 用途 | 色值 |
|------|------|
| 主品牌黄（CTA核心）| `#FFE234` 电光柠檬黄 |
| 深海军蓝（主背景/文字）| `#1A2B6B` |
| 鸭嘴橙（强调/eyebrow）| `#F4820A` |
| 薄荷绿（成功/勾选）| `#00BFA5` |
| 暖米白（页面底色）| `#F9F8F5`（非纯白，有纸张温度）|

### ✍️ 字体
- **主字体**：DM Sans（圆润、现代、可读性极佳）
- **代码字体**：DM Mono
- H1 字号 72px，letter-spacing `-0.03em`，超紧缩行高 `1.15`
- 正文行高 `1.7`，颜色用 `#5C5A53`（暖灰，非冷灰）

### 🃏 最标志性的视觉细节
**Offset Card Shadow**（扁平立体阴影）：
```css
box-shadow: 3px 3px 0 0 #1A2B6B;   /* 默认 */
box-shadow: 5px 5px 0 0 #1A2B6B;   /* hover，配合 translate(-2px,-2px) */
```
这个扁平位移阴影配合 `border: 2px solid #1A2B6B` 创造出强烈的扁平插画感，是 MotherDuck 最辨识度高的设计语言。

### 📐 间距与圆角
- 基于 **4pt 网格**系统（8/16/24/32/40/48px...）
- 卡片圆角 `24px`（radius-xl），按钮 `10px`（radius-md）
- 页面 padding 宽松，Hero 上下 `96px`

### 🧩 组件特征
- Bento Grid 非等列布局（1+2跨列组合）
- Badge/Tag 使用浅色背景 + 细描边（非实心）
- 按钮必须有 `border` + `box-shadow` 的组合，hover 产生"抬起"动效
- 评价区用无限滚动跑马灯